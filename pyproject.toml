[build-system]
requires = ["setuptools"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "mad-py"
version = "0.1.0"
requires-python = ">=3.11"

[tool.setuptools]
py-modules = ["mad"]
