[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "mad"
version = "0.1.0"
description = "fast binary serializer"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]